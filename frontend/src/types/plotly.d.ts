/**
 * Type declarations for plotly.js partial bundle.
 * The partial bundle doesn't ship its own types, so we reference the main plotly.js types.
 */
declare module "plotly.js-cartesian-dist-min" {
  import Plotly from "plotly.js";
  export = Plotly;
}
